# BaseRift – copy to .env.local and fill values.
# Base Build docs: https://docs.base.org/mini-apps/quickstart/migrate-existing-apps

# Required for development
MINIAPP_DOMAIN=localhost:3000
MINIAPP_HOME_URL=http://localhost:3000
MINIAPP_ICON_URL=http://localhost:3000/icon.png
MINIAPP_SPLASH_URL=http://localhost:3000/splash.png
MINIAPP_WEBHOOK_URL=http://localhost:3000/api/webhook

# Dev mode (set to false in production)
DEV_AUTH_BYPASS=true
NODE_ENV=development

# Session (generate: openssl rand -base64 32)
SESSION_SECRET=dev-secret-change-in-production

# Account association – REQUIRED for production.
# Generate at Base Build: https://www.base.dev/preview → Account association tab.
# Paste your app URL, click Submit, then Verify; copy header, payload, signature into Vercel env.
# If you see "dummy testing account" next to your app in base.dev preview, create a NEW association
# linked to your real Base/Farcaster account (not the test option) and set the new values here.
# Omit if not set (manifest will omit accountAssociation).
ACCOUNT_ASSOCIATION_HEADER=
ACCOUNT_ASSOCIATION_PAYLOAD=
ACCOUNT_ASSOCIATION_SIGNATURE=

# Neynar (webhook verification) – https://neynar.com
NEYNAR_API_KEY=

# Base Pay treasury address
BASE_PAY_TREASURY_ADDRESS=0x0000000000000000000000000000000000000000

# Contracts (after deployment)
COSMETICS_CONTRACT_ADDRESS=
MINTER_PRIVATE_KEY=

# Public (Base Sepolia)
NEXT_PUBLIC_CHAIN_ID=84532
NEXT_PUBLIC_RPC_URL=https://sepolia.base.org
NEXT_PUBLIC_URL=http://localhost:3000
